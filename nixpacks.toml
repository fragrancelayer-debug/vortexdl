[phases.setup]
nixPkgs = ["nodejs_20", "python311", "ffmpeg"]
cmds = ["pip3 install yt-dlp --break-system-packages"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
